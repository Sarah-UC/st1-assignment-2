# Human vs AI Comparison

| Question | Human version | AI version |
|---|---|---|
| **Easy to understand?** | Yes. It is simple and includes a separate function for displaying appointments. | Yes. It is also simple, but printing the whole list is less user-friendly. |
| **Runs successfully?** | Yes. | Yes. |
| **Uses only required features?** | Yes. | Yes. |
| **Adds assumptions?** | Slightly. It assumes a patient name must not be empty. | Slightly. It assumes appointments should simply be added without validation. |
| **Handles errors?** | Yes. It checks for an empty patient name. | No. It does not validate the inputs. |
| **Could I explain it?** | Yes. | Yes, after reading and understanding Copilot's explanation. |

## Conclusion

After comparing the two versions, I find both versions easy enough to understand and both run successfully. My version is slightly better at handling errors because I added a check for an empty patient name and a function to display the appointments.

The AI version is simpler and easier to read because it uses a straightforward function, but it does not include any input validation. It also prints the entire appointments list directly, which is less readable.

The AI version did not use a database or GUI, so it followed the requirements. I can explain both versions after going through the code carefully.
